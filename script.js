// Esperar a que el documento esté listo
$(document).ready(function() {
    // Evento clic para el botón
    $("#ocultar-btn").click(function() {
        // Ocultar el párrafo usando el método hide()
        $("#parrafo").hide();
    });
});